[project]
name = "tensor-decomposer"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "django>=6.0.7",
    "numpy>=2.5.1",
    "pytest>=9.1.1",
    "scikit-learn>=1.9.0",
]
